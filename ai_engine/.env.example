# Backend (ai_engine) environment variables
ZH_HOST=0.0.0.0
ZH_PORT=8000
# Comma-separated list of allowed origins for CORS.
# In production, set to the deployed frontend URL, e.g.:
# ZH_ALLOWED_ORIGINS=https://zombiehunter.vercel.app
ZH_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
