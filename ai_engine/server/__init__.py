"""ZombieHunter inference API.

Loads the three trained components from train/models/<component>/artifacts/,
runs inference once on the lifecycle training set, and serves the resulting
endpoint inventory to the React frontend.
"""
