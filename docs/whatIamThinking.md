- first things first has to keep a local slm as the system is going to be active for always 
- second most of the data flow i am going to keep in rust only the ai process in pythonthat too deployed by rust engines 
- since using slms has to make ml/dl models to keep the reponse deterministic and proper so that slm can understand properly 
    - problem here will be datasets and whaat all models 

- data from all the three sources would be in json and my rust engine will pick it up from there and throw the analysis again in json and the backend engineer will pick it up from there 
- frontend engineer throws data in json me being ai engineer will pick it up from there 
- and also for the sake of usp want to integrate vision models also 

- and at last work with as least compute as possible to make it bussiness viable 
